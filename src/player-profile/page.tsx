// Required Features:

// Display relevant stats (season total vs. per game toggle).

// Display all scouting reports.

// Allow adding new scouting reports via a form (using useState, no backend needed).

// Link back to the Big Board.

export function PlayerProfile(){
    return(
        <>
        </>
    )
}